const { saveUser,getConfig,getBotMedia }=require('../config/db');const { replaceVars }=require('../utils');const { Markup }=require('telegraf');
module.exports=bot=>bot.start(async ctx=>{await saveUser(ctx.from.id,{username:ctx.from.username||'',first_name:ctx.from.first_name||'',last_name:ctx.from.last_name||''});const c=await getConfig(),m=await getBotMedia(),t=replaceVars(c.bienvenida_texto||'👋 ¡Hola {mencion}! 💎\n\nBienvenid@ a VerifiedModels ✨',ctx),kb=Markup.inlineKeyboard([[Markup.button.webApp('💎 Galería Virtual 💎',process.env.WEBAPP_URL||'')],[Markup.button.callback('👑 Lista de Modelos 👑','public_modelos')],...(process.env.CANAL_FREE_URL?[Markup.button.url('📢 Canal FREE 📢',process.env.CANAL_FREE_URL)]:[])]);if(m.bienvenida)return ctx.replyWithPhoto(m.bienvenida,{caption:t,parse_mode:'HTML',...kb});return ctx.reply(t,{parse_mode:'HTML',...kb});});